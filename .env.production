# Production build (npm run build:production).
# Host comes from window.location.origin at runtime.
VITE_APP_BASE_PATH=
VITE_API_PATH=/ghoulhrms/api/v1

# Optional full URL override (unusual — prefer VITE_API_PATH + origin):
# VITE_API_BASE_URL=https://peopleaiq.com/ghoulhrms/api/v1
