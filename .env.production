# Optional override. When unset, production builds derive the API URL from
# window.location.origin + /ghoulhrms/api/v1 (matches nginx proxy config).
# VITE_API_BASE_URL=https://peopleaiq.com/ghoulhrms/api/v1
